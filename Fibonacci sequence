#include <stdio.h>

int main(){

    int x = 1;
    int y = 2;
    int z = 3;
    int temp;

    printf("%d\n", x);
    printf("%d\n", y);

    for(int i = 1; i < 20; i++){
        printf("%d\n", z);
        x = y;
        y = z;
        z = x + y;
    }
}
