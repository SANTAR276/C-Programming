#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(){

    int *pNum = malloc(3 * sizeof(int));

    pNum[0] = 3;
    pNum[1] = 6;
    pNum[2] = 9;

    printf("The values are %d, %d, and %d", pNum[0], pNum[1], pNum[2]);

    free(pNum);
    
    return 0;
}
