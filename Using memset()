#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct _Point2D{
    int x;
    int y;
} Point2D;

int main(){

    Point2D p1;
    
    memset(&p1, 0, sizeof(Point2D));
    
    printf("%d %d", p1.x, p1.y);

    return 0;
}
