#include <stdio.h>
#include <ctype.h>

int main(){

    char a = 'R';

    if(isalpha(a)){
        printf("It's an alphabet");
    }
    else if(isdigit(a)){
        printf("It's a number");
    }
    else{
        printf("It's not a number or an alphabet");
    }
    return 0;
}
