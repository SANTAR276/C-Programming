#include <stdio.h>

int main(){
    double nums[5];

    for(int i = 0; i < 5; i++){
        printf("What number do you want?: ");
        scanf("%lf", &nums[i]);
    }
    
    for(int j = 0; j < 5; j++){
        printf("%.1f\n", nums[j]);
    }
    return 0;
}
