#define _CRT_OBSOLETE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(){

	char *script = "I missed the part where that's my problem";

	int length = strlen(script);

	for(int i = 0; i < length; i++){
		printf("%c", script[i]);

		_sleep(100);
	}

	return 0;

}
