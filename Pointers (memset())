#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(){

    long long *pNum = malloc(sizeof(long long));

    memset(pNum, 0x27, 8);

    printf("0x%llx\n", *pNum);
    
    free(pNum);
    
    return 0;
}
