#include <stdio.h>

int main(){
    int num = 0;
    int sum = 0;

    do{
        printf("Enter a number above 0: ");
        scanf("%d", &num);
        sum = sum + num;
    }while(num > 0);

    printf("%d", sum);

    return 0;
}
