#include <stdio.h>


int main(){

    char buffer[25];

    FILE *fp = fopen("tests3.txt", "r");

    fgets(buffer, sizeof(buffer), fp);

    printf("%s\n", buffer);

    fclose(fp);

    return 0;
}
